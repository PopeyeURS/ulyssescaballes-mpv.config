# commands.conf
# Define custom keybindings or command aliases

# Example: Bind "Ctrl+Shift+S" to take a screenshot
bind_ctrl_shift_s=screenshot

# Example: Bind "Alt+M" to toggle midrange boost
bind_alt_m=script-binding midrange_boost/midrange_boost

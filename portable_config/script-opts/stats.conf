# stats.conf
# Customize which playback stats are shown on screen

# Show CPU usage
show_cpu=yes

# Show GPU usage
show_gpu=yes

# Show dropped frames
show_dropped_frames=yes

# Show cache status
show_cache=yes

# Show video/audio sync offset
show_avsync=yes

# Refresh rate in milliseconds
refresh_rate=500
